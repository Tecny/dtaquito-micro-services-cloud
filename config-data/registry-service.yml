server:
  port: 8099

eureka:
  instance:
    hostname: ${EUREKA_HOST:registry-service}
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname:${EUREKA_HOST:localhost}}:${server.port:${EUREKA_PORT:8761}}/eureka/
