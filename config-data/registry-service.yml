server:
  port: 8099

eureka:
  instance:
    hostname: dtaquito-registry-service.azurewebsites.net
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:https://dtaquito-registry-service.azurewebsites.net/eureka}
