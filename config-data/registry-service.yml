server:
  port: 8099

eureka:
  instance:
    hostname: ${EUREKA_HOST:localhost}
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: ${eureka.instance.hostname:${EUREKA_HOST:localhost}}:${server.port:${EUREKA_PORT:8761}}/eureka/
