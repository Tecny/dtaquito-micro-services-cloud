server:
  port: 8099

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
#       defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
       defaultZone: ${EUREKA_SERVER:http://registry-service:8099/eureka}
