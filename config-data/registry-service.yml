server:
  port: 8099

eureka:
  instance:
    hostname: ${EUREKA_HOST:registry-service}
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: ${EUREKA_HOST:http://registry-service:8099/eureka}
